# cpp-project
CPP Sample Program

Build and run

Compile (links math library):

```
gcc -g main.cpp -o main -lm
```

Run with input (example):

```
printf "sin10x\n" | ./main
```

Or use the VS Code build task (it now links `-lm`).
